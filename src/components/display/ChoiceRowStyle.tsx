import styled from "styled-components"

export const ChoiceRowWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 10px;

  & > input[type="radio"] {
    margin-left: auto;
  }
`
